import { Profile } from './profile.model';

export interface Product {
  favorited: any;
  id: any;
  favorites: boolean;
  name: string;
  description: string;
  categories: string;
  corporativo: boolean;
}
